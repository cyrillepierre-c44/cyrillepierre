<% content_for :title, @actu.display_title + " — Cyrille PIERRE" %>
<% content_for :description, @actu.excerpt(length: 155) %>
<% content_for :head do %><%= structured_data_tag(actu_schema(@actu)) %><% end %>

<section class="service-hero">
  <div class="container">
    <%# Une actu publiée porte normalement sa date, mais rien en base ne l'impose : une valeur
   manquante ne doit pas faire tomber une page publique. %>
    <span class="section-eyebrow">
      <%= @actu.kind_label %> · <%= (@actu.published_at || @actu.updated_at).strftime("%d/%m/%Y") %>
    </span>
    <h1><%= @actu.display_title %></h1>
  </div>
</section>

<div class="container pb-5">
  <div class="actu-body">
    <%= ArticleFormatter.call(@actu.output) %>
  </div>

  <%= link_to "← Toutes les actus", actus_path, class: "studio-back-link" %>
</div>
